#include "Animation.h"

/*SDL_Texture* Animation::getCurrentFrame(double timeSinceStart)
{
	int frameIndex = (static_cast<int>(timeSinceStart / _animTime) * _numSprites) % _numSprites;
	return _sprites[frameIndex].getTexture();
}*/